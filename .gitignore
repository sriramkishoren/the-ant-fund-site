node_modules
dist
.DS_Store
*.log
.vite
.env
.env.local

# Claude Code session metadata
.claude/

# Raw working assets at the project root. The processed/optimized copies
# live in /public; these are the unmodified source files.
fav-icons/
ant_logo*
